package de.l3s.eventkg.integration;

public class EntityIdGenerator {

	public void initEntityIdMapping() {

		// TODO

		// BufferedReader br = null;
		// try {
		// try {
		// br =
		// FileLoader.getReader(FileName.ALL_TTL_ENTITIES_WITH_TEXTS_PREVIOUS_VERSION);
		// } catch (FileNotFoundException e1) {
		// e1.printStackTrace();
		// }
		//
		// String line;
		// while ((line = br.readLine()) != null) {
		//
		// if(line.isEmpty()||line.startsWith("@"))
		// continue;
		//
		// String[] parts = line.split("\t");
		// int id=parts[0].substring(beginIndex, endIndex)
		//
		// }
		// } catch (IOException e) {
		// e.printStackTrace();
		// } finally {
		// try {
		// br.close();
		// } catch (IOException e) {
		// e.printStackTrace();
		// }
		// }

	}

}
